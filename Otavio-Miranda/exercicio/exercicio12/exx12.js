// escreva uma função chamada ePaisagem que receb dois argumentos, largura e altura de uma imagem(number), retorne true se a imagem estiver no modo paisagem

const ePaisagem = (l, h)  =>  l > h ;

console.log(ePaisagem(8000, 500))